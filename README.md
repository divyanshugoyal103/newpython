# newpython

## bNode core engine

This repository now includes a pure `bNode` hierarchical tree engine in `bnode.py`.

- Output: hierarchical tree structure via `to_dict()` or `to_lines()`.
- No Chrome API usage.
- No side effects (immutable node operations).
